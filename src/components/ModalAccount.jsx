import React from 'react'
import Modal from './modal'


function ModalAccount({title,root}) {
  return (
    <Modal title={title} root={root}> Modal de cuenta</Modal>
  )
}

export default ModalAccount
